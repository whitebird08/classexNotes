// Your program should emit the longest valid English language word
// ( http://norvig.com/ngrams/enable1.txt ) 
// you can make for each keyboard configuration.
// abcd = bacaba
// qwer = ewerer
// hjklo = kolokolo
// Challenge Input

// 4
// edcf
// bnik
// poil
// vybu
// Challenge Output

// edcf = deedeed
// bnik = bikini
// poil = pililloo
// vybu = bubby

var validDictionary = [a, and, or, is, not, knot, deedeed, bikini, pililloo, bubby] ahttp://norvig.com/ngrams/enable1.txt )
var allCombos = [];
var validWords = [];
var longestWord = [];

//make all combinations

//check if valid
if (string = validDictionary[i]){
  arr.push
}
//compare length
if arr[i].length < arr[i-1]

function shuffleString(string){
  //builds all possible combinations of string
  // pushes each to allCombos array
}

function compareWithDictionary(){
  //compares allCombos[i] to validDictionary
  if (allCombos[i] === validDictionary[i]){
    validWords.push(allCombos[i])
  }

function(){
  validWords.map
  if(validWords[i].length > validWords[i-1].length)
    longestWord.splice(0,1[validWords.[i]])
  }

//////
var text = fs.readFileSync('text.text').toString()split('\n')
var validDictionary = [a, and, or, is, not, knot, deedeed, bikini, pililloo, bubby] ahttp://norvig.com/ngrams/enable1.txt )
var validWords = [];
var longestWord = [];

//make all combinations

//check if valid
if (string = validDictionary[i]){
  arr.push
}
//compare length
if arr[i].length < arr[i-1]

function compareWithDictionary(){
  //compares allCombos[i] to validDictionary (fs.readfile)
  if (allCombos[i] === validDictionary[i]){
    validWords.push(allCombos[i])
  }

function determineLongest(){
  validWords.map(function(){
    if(validWords[i].length > validWords[i-1].length){
      longestWord.splice(0,1[validWords.[i]])
    }
  })
}



// time node script.js
// to display time












